model: densenet121
dataset: cifar10

epochs: 100
batch_size: 512
seed: 42

optimizer: SGD

schedulers: [none, cyclic, steplr, cosine]

clr_policies: [triangular, triangular2, exp_range]
clr_gamma: 0.99994  # (Smith arXiv:1506.01186)
lr_bounds:  # Smith Table 4
  - [0.1, 0.2]
  - [0.1, 0.3]
step_sizes: [4, 8] # epochs per half-cycle; paper: cycle_length = max_epochs/10 -> stepsize = 5

fixed_lrs: [0.1, 0.2]  # Table 4 fixed LRs of 0.1, 0.2, 0.3

steplr_initial_lrs: [0.1, 0.05]
steplr_step_sizes: [25, 40]  # decays at [25,50,75] and [40,80] — standard 50%/75% milestones
steplr_gammas: [0.1, 0.2]

cosine_initial_lrs: [0.1, 0.05]
cosine_t_max: [100]  # total epochs for cosine cycle
cosine_eta_min: [0.0, 0.001]  # minimum LR
